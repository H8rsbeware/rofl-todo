[project]
name = "rofl-projector"
version = "0.1.0"

dependencies = [
    "rofl @ git+https://github.com/H8rsbeware/rofl.git"
]


[tool.basedpyright]
venvPath = "."
venv = ".venv"


[project.scripts]
projector = "project:main"
