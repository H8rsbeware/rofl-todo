[project]
name = "rofl-todos"
version = "0.1.0"

dependencies = [
    "rofl @ git+https://github.com/H8rsbeware/rofl.git"
]


[tool.basedpyright]
venvPath = "."
venv = ".venv"


[project.scripts]
todos = "manager:main"
